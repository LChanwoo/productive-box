name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: LChanwoo/*
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GIST_ID: 9571818c711328906962d029288334a0
          TIMEZONE: Asia/Seoul
